name: Build BLE Firmware

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: zmkfirmware/zmk-build-arm:stable
    name: Build BLE Keyboard
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      
      - name: Install Python setuptools
        run: |
          apt-get update
          apt-get install -y python3-setuptools
          
      - name: Create BLE configuration with display
        run: |
          # Создаем конфигурацию с поддержкой дисплея
          cat > config/sofle.conf << 'EOF'
          # Basic BLE Configuration
          CONFIG_ZMK_BLE=y
          CONFIG_ZMK_SPLIT_BLE_ROLE_CENTRAL=y
          CONFIG_ZMK_SPLIT=y
          CONFIG_BT_CTLR_TX_PWR_PLUS_8=y
          
          # Basic settings
          CONFIG_ZMK_USB=y
          
          # Display configuration
          CONFIG_ZMK_DISPLAY=y
          CONFIG_ZMK_DISPLAY_STATUS_SCREEN_BUILT_IN=y
          CONFIG_ZMK_WIDGET_BATTERY_STATUS=y
          CONFIG_ZMK_WIDGET_OUTPUT_STATUS=y
          CONFIG_ZMK_WIDGET_LAYER_STATUS=y
          CONFIG_ZMK_WIDGET_WPM_STATUS=y
          
          # LVGL configuration
          CONFIG_LVGL=y
          CONFIG_LVGL_USE_LABEL=y
          CONFIG_LVGL_USE_CONT=y
          CONFIG_LVGL_HOR_RES=128
          CONFIG_LVGL_VER_RES=32
          CONFIG_LVGL_DPI=130
          CONFIG_LVGL_BITS_PER_PIXEL=1
          CONFIG_LVGL_DISPLAY_DEV_NAME="DISPLAY"
          
          # Disable ext_power to avoid errors
          CONFIG_ZMK_EXT_POWER=n
          EOF
          
          echo "=== BLE Configuration with Display ==="
          cat config/sofle.conf
      
      - name: Cache west modules
        uses: actions/cache@v4
        env:
          cache-name: cache-zephyr-modules
        with:
          path: |
            modules/
            tools/
            zephyr/
            bootloader/
            zmk/
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/west.yml') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-
      
      - name: West Init
        run: west init -l config
      
      - name: West Update
        run: west update
      
      - name: West Zephyr export
        run: west zephyr-export
      
      - name: Build Left Half (Central)
        run: |
          west build -s zmk/app -b nice_nano_v2 -- \
            -DSHIELD=sofle_left \
            -DZMK_CONFIG="${GITHUB_WORKSPACE}/config"
      
      - name: Rename Left Firmware
        run: cp build/zephyr/zmk.uf2 sofle_left_ble_central.uf2
      
      - name: Clean build
        run: rm -rf build
      
      - name: Build Right Half (Peripheral)
        run: |
          west build -s zmk/app -b nice_nano_v2 -- \
            -DSHIELD=sofle_right \
            -DZMK_CONFIG="${GITHUB_WORKSPACE}/config" \
            -DCONFIG_ZMK_SPLIT_BLE_ROLE_CENTRAL=n
      
      - name: Rename Right Firmware
        run: cp build/zephyr/zmk.uf2 sofle_right_ble_peripheral.uf2
      
      - name: Upload BLE Firmware
        uses: actions/upload-artifact@v4
        with:
          name: sofle-ble-firmware-with-display
          path: |
            sofle_left_ble_central.uf2
            sofle_right_ble_peripheral.uf2